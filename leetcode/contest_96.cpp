#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
    int proj(vector<vector<int>> & grid) {
        //cout << grid.size() << endl;
        //cout << grid[0].size() << endl;
        int col = grid.size();
        int row = grid[0].size();
        for(int )

        return 0;
    }

};

int main()
{
    Solution ss;
    vector<vector<int>> arry(1);
    arry[0].push_back(2);
    cout << ss.proj(arry) << endl;
}
